[package]
name = "darkbio_crypto_ffi"
version = "0.19.2"
edition = "2024"
license = "BSD-3-Clause"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
darkbio-crypto = { version = "0.19.1", features = ["argon2", "cbor", "cose", "cwt", "hkdf", "rand", "rsa", "stream", "xdsa", "xhpke"] }
flutter_rust_bridge = "=2.13.0"
hex = "0.4"

[profile.release]
lto = true
opt-level = "z"
strip = true

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }
